※ 람다(lambda)

- 런타임에 생성해서 사용할 수 있는 익명 함수
- 쓰고 버리는 일시적인 함수
- 함수가 생성된 곳에서만 필요
  
   lambda 인자 : 표현식

ex) 두 수를 더하는 함수

	- 일반적인 방법
	   def hap(x, y):
		return x + y

	   hap(10, 20)

	- 람다
	  (lambda x, y : x + y)(10, 20)